
/**
* Example Utility Function
* Used for example of where to put custom business logic within the app.
* Also used for example of how to do automated tests!
*/
export function ExampleUtilFunction(input: string): string {
    return `Hello ${input}`;
}
